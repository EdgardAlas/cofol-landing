---
import IconMenu from './icons/IconMenu.astro';
import IconClose from './icons/IconClose.astro';
import LanguagePicker from './LanguagePicker.astro';
import { getCurrentLocale, getLocaleUrl } from '../i18n/utils';
import { common } from '../i18n/translations/common';

const lang = getCurrentLocale(Astro.currentLocale);
const t = common[lang];
const base = getLocaleUrl(lang);

const navLinks = [
  { label: t.nav.genesis, href: `${base}#genesis` },
  { label: t.nav.ecosystem, href: `${base}#ecosystem` },
  { label: t.nav.philosophy, href: `${base}#philosophy` },
  { label: t.nav.contact, href: `${base}#contact` },
];
---

<header
  id="site-header"
  class="sticky top-0 z-50 bg-neutral-100/80 backdrop-blur-md transition-shadow duration-300"
>
  <nav
    class="container mx-auto grid grid-cols-2 items-center px-4 py-4 sm:px-6 md:grid-cols-3 lg:px-8"
  >
    <div class="flex justify-start">
      <a
        href={getLocaleUrl(lang)}
        class="font-heading text-xl font-extrabold tracking-tight text-neutral-950"
      >
        COFOL
      </a>
    </div>

    <div class="hidden justify-center md:flex">
      <ul class="flex items-center gap-8">
        {
          navLinks.map((link) => (
            <li>
              <a
                href={link.href}
                class="nav-link font-heading data-active:border-primary-600 data-active:text-primary-600 border-b-2 border-transparent text-sm font-semibold text-neutral-500 transition-colors hover:text-neutral-950"
              >
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </div>

    <div class="flex items-center justify-end gap-2">
      <LanguagePicker />
      <button
        id="menu-toggle"
        aria-label="Toggle menu"
        aria-expanded="false"
        class="flex size-10 items-center justify-center rounded-lg text-neutral-950 transition-colors hover:bg-neutral-200 md:hidden"
      >
        <IconMenu id="icon-menu" />
        <IconClose id="icon-close" class="hidden" />
      </button>
    </div>
  </nav>

  <div
    id="mobile-menu"
    class="absolute inset-x-0 top-full grid bg-neutral-100/95 backdrop-blur-md md:hidden"
    style="grid-template-rows: 0fr; transition: grid-template-rows 0.3s ease;"
  >
    <div class="overflow-hidden">
      <div class="border-t border-neutral-200">
        <ul class="container mx-auto flex flex-col px-4 py-2 sm:px-6">
          {
            navLinks.map((link) => (
              <li class="mobile-link">
                <a
                  href={link.href}
                  class="mobile-nav-link font-heading data-active:border-primary-600 data-active:text-primary-600 block border-l-2 border-transparent py-3 pl-3 text-sm font-semibold text-neutral-500 transition-colors hover:text-neutral-950"
                >
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
  </div>
</header>

<script>
  import { initMobileMenu } from '../lib/navbar/mobile-menu';
  import { initScrollShadow } from '../lib/navbar/scroll-shadow';
  import { initActiveLinks } from '../lib/navbar/active-links';

  initMobileMenu();
  initScrollShadow();
  initActiveLinks();
</script>
